---
layout: null
---
{
    "version": "https://jsonfeed.org/version/1.1",
    "title": "{{ site.title | xml_escape }}",
    "description": "{{ site.description | xml_escape }}",
    "home_page_url": "{{ "/" | absolute_url }}",
    "feed_url": "{{ "/feed.json" | absolute_url }}",
    "user_comment": "This feed allows you to read the posts from this site in any feed reader that supports the JSON Feed format. To add this feed to your reader, copy the following URL — {{ "/feed.json" | absolute_url }} — and add it your reader.",
    "favicon": "{{ "/assets/images/favicon.ico" | absolute_url }}",
    "icon": "{{ "/assets/images/logo.png" | absolute_url }}",
    "author": {
        "name": "{{ site.author.name }}",
        "url": "{{ site.author.links[0].url }}"
    },
    "items": [
        {% for post in site.posts limit: 20 %}
        {
            "id": "{{ post.url | absolute_url | xml_escape }}",
            "url": "{{ post.url | absolute_url | xml_escape }}",
            "title": "{{ post.title | xml_escape }}",
            "content_html": {{ post.content | jsonify }},
            "summary": "{{ post.excerpt | strip_html | truncatewords: 50 | xml_escape }}",
            "date_published": "{{ post.date | date_to_xmlschema }}",
            {% if post.modified %}
            "date_modified": "{{ post.modified | date_to_xmlschema }}",
            {% endif %}
            {% if post.author %}
            "author": {
                "name": "{{ post.author | xml_escape }}"
            },
            {% endif %}
            "tags": [
                {% for tag in post.tags %}
                "{{ tag | xml_escape }}"{% unless forloop.last %},{% endunless %}
                {% endfor %}
            ]
        }{% unless forloop.last %},{% endunless %}
        {% endfor %}
    ]
}
